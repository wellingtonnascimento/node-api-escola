require('dotenv').config();

module.exports = {
  dialect: 'mariadb',
  host: process.env.DATABASE_HOST,
  port:
  username:
  password:
  database:
  define:{},
  dialectOptions: {},
  timezone: 'America/Sao_Paulo'
};
